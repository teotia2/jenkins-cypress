stages:
  - test

cypress-tests:
  image: cypress/included:13.1.0
  stage: test
  script:
    - npm ci
    - npm test
    - npx cypress run
  artifacts:
    when: always
    paths:
      - cypress/screenshots
      - cypress/videos
    expire_in: 1 week
